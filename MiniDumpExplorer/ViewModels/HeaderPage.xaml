<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MiniDumpExplorer.HeaderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MiniDumpExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Name="sv" Margin="5">
        <Grid MinWidth="{Binding ElementName=sv, Path=ViewportWidth}" MinHeight="{Binding ElementName=sv, Path=ViewportHeight}" ColumnSpacing="5" RowSpacing="5" Margin="12,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                <Image Height="20" Width="20" Source="{x:Bind FileItem.Icon,Mode=OneWay}" Margin="5,0,0,0"/>
                <TextBlock Text="{x:Bind FileItem.Name,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />
            </StackPanel>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Location" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind FileItem.Location,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Size" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            <TextBlock x:Name="headerFileSize" Grid.Row="2" Grid.Column="1" Text="{x:Bind FileItem.Size,Converter={StaticResource SizeNumberConverter},Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Dump Type" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            <TextBlock Grid.Row="3" Grid.Column="1" Text="{x:Bind DumpType,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Signature" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="{x:Bind Signature,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />
                <TextBlock Text="Invalid" Foreground="Red" Visibility="{x:Bind IsValid,Converter={StaticResource BoolToVisibilityConverter},ConverterParameter=True}" Margin="10,0,0,0" />
            </StackPanel>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Version" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}" />
            <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock x:Name="headerVersion" Text="{x:Bind Version,Converter={StaticResource HexNumberConverter},ConverterParameter=full,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />
                <TextBlock Text="Internal" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Margin="20,0,0,0" />
                <TextBlock x:Name="headerInternalVersion" Text="{x:Bind InternalVersion,Converter={StaticResource HexNumberConverter},ConverterParameter=full,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" Margin="5,0,0,0" />
            </StackPanel>

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Number Of Streams" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBlock Grid.Row="6" Grid.Column="1" Text="{x:Bind NumberOfStreams,Converter={StaticResource LocalNumberConverter},Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}" IsTextSelectionEnabled="True" />

            <TextBlock Grid.Row="7" Grid.Column="0" Text="Stream Directory RVA" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBlock x:Name="headerStreamDirectoryRva" Grid.Row="7" Grid.Column="1" Text="{x:Bind StreamDirectoryRva,Converter={StaticResource HexNumberConverter},ConverterParameter=full,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}" IsTextSelectionEnabled="True" />

            <TextBlock Grid.Row="8" Grid.Column="0" Text="CheckSum" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}" />
            <StackPanel Grid.Row="8" Grid.Column="1" Orientation="Horizontal" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock x:Name="headerCheckSum" Text="{x:Bind CheckSum,Converter={StaticResource HexNumberConverter},ConverterParameter=full,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />
                <TextBlock Text="File CRC32" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Margin="20,0,0,0" />
                <TextBlock x:Name="headerDumpFileCrc32" Text="{x:Bind DumpFileCrc32,Converter={StaticResource HexNumberConverter},ConverterParameter=full,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" Margin="5,0,0,0" />
            </StackPanel>

            <TextBlock Grid.Row="9" Grid.Column="0" Text="Timestamp Local" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}" />
            <StackPanel Grid.Row="9" Grid.Column="1" Orientation="Horizontal" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="{x:Bind TimeDateStamp,Converter={StaticResource TimeStampNumberConverter},Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />
                <TextBlock Text="UTC" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Margin="20,0,0,0" />
                <TextBlock Text="{x:Bind TimeDateStamp,Converter={StaticResource TimeStampNumberConverter},ConverterParameter=True,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" Margin="5,0,0,0" />
            </StackPanel>

            <TextBlock Grid.Row="10" Grid.Column="0" Text="Flags" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBlock x:Name="headerFlags" Grid.Row="10" Grid.Column="1" Text="{x:Bind Flags,Converter={StaticResource HexNumberConverter},ConverterParameter=full,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}" IsTextSelectionEnabled="True" />
            <ListBox Grid.Row="11" Grid.Column="1" ItemsSource="{x:Bind FlagsList,Mode=OneWay}" Visibility="{x:Bind IsUserDump,Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,5,0,0" />
        </Grid>
    </ScrollViewer>
</Page>
