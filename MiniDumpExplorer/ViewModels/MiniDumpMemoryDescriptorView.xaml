<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MiniDumpExplorer.MiniDumpMemoryDescriptorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MiniDumpExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnSpacing="5" RowSpacing="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Start" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
        <TextBlock x:Name="startOfMemoryRange" Grid.Row="0" Grid.Column="1" Text="{x:Bind Value.StartOfMemoryRange,Converter={StaticResource HexNumberConverter},ConverterParameter=full,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />
        <TextBlock Grid.Row="1" Grid.Column="0" Text="End" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
        <TextBlock x:Name="endOfMemoryRange" Grid.Row="1" Grid.Column="1" Text="{x:Bind Value.EndOfMemoryRange,Converter={StaticResource HexNumberConverter},ConverterParameter=full,Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Size" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
        <TextBlock x:Name="dataSize" Grid.Row="2" Grid.Column="1" Text="{x:Bind Value.DataSize,Converter={StaticResource SizeNumberConverter},Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" IsTextSelectionEnabled="True" />
    </Grid>
</UserControl>
