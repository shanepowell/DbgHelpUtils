<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MiniDumpExplorer.ThreadStackView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MiniDumpExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="StackEntryViewTemplate" x:DataType="local:ThreadStackEntry">
            <TreeViewItem ItemsSource="{x:Bind Children}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{x:Bind Line,Mode=OneWay}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                </Grid>
            </TreeViewItem>
        </DataTemplate>
    </UserControl.Resources>

    <Grid ColumnSpacing="5" RowSpacing="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- 0 -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Stack Memory" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Visibility="{x:Bind Stack.HasStackMemoryRange,Converter={StaticResource BoolToVisibilityConverter},Mode=OneWay}" />
        <local:MiniDumpMemoryDescriptorView Grid.Row="0" Grid.Column="1" Value="{x:Bind Stack.StackMemoryRange}" Visibility="{x:Bind Stack.HasStackMemoryRange,Converter={StaticResource BoolToVisibilityConverter},Mode=OneWay}" />
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Call Stack" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Margin="0,15,0,0" />
        <TreeView 
            x:Name="stackEntries"
            Grid.Row="1"
            Grid.Column="1"
            SelectionMode="Single"
            ItemsSource="{x:Bind Stack.StackEntries,Mode=OneWay}"
            ItemTemplate="{StaticResource StackEntryViewTemplate}"
            Expanding="OnTreeViewItemExpanding"
            Margin="0,5,0,0"
        />
    </Grid>
</UserControl>
