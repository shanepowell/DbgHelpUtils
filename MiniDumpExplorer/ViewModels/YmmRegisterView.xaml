<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MiniDumpExplorer.YmmRegisterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MiniDumpExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnSpacing="5" RowSpacing="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:M128AView Grid.Row="0" Grid.Column="0" Value="{x:Bind Value.Ymm, Mode=OneWay}" />
        <local:M128AView Grid.Row="0" Grid.Column="1" Value="{x:Bind Value.Xmm, Mode=OneWay}" />
    </Grid>
</UserControl>
