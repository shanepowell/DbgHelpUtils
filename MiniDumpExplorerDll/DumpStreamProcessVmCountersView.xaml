<UserControl
    x:Class="MiniDumpExplorer.DumpStreamProcessVmCountersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MiniDumpExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="TrueToVisibleConverter" />
        <local:BooleanToVisibilityConverter x:Key="FalseToVisibleConverter" IsReversed="True"/>
        <local:NumberConverter x:Key="NumberConverter"/>
        <local:IsEqualToVisibilityConverter x:Key="VersionOneConverter" Value="1"/>
        <local:IsEqualToVisibilityConverter x:Key="VersionTwoConverter" Value="2"/>
    </UserControl.Resources>

    <ScrollViewer HorizontalScrollBarVisibility="Auto" 
                  VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" HorizontalTextAlignment="Left">Stream Process VM Counters</TextBlock>
            <TextBlock Grid.Row="1" Grid.Column = "0" VerticalAlignment="Center" Style="{StaticResource BaseTextBlockStyle}">Index</TextBlock>
            <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource BodyTextBlockStyle}"
                     Background="Transparent"
                     BorderThickness="0"
                     Text="{x:Bind Path=Stream.Index, Converter={StaticResource NumberConverter}}"
                     FontFamily="Courier New"
                     IsReadOnly="True"
                     TextWrapping="Wrap" />
            <local:ProcessVmCounters1View VmCounters="{x:Bind Path=Stream.ProcessVmCounters1}" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Visibility="{x:Bind Path=Stream.ProcessVmCountersVersion, Converter={StaticResource VersionOneConverter}}" />
            <local:ProcessVmCounters2View VmCounters="{x:Bind Path=Stream.ProcessVmCounters2}" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Visibility="{x:Bind Path=Stream.ProcessVmCountersVersion, Converter={StaticResource VersionTwoConverter}}" />
        </Grid>
    </ScrollViewer>
</UserControl>
